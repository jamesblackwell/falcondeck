# Mini Zen

Mini Zen is FalconDeck's official proof extension for full-main-area panels.
It begins as a static declarative panel and will gain the bounded events and
`threads:read` facets in later Phase 6 slices.

The extension uses only `@falcondeck/extension-sdk`; clients render the
manifest document without executing extension code.
